[tool.pytest.ini_options]
# "." puts the project root on sys.path so `tools.*` imports resolve no matter
# which directory pytest is invoked from; the second entry lets tests import
# `logic.*` straight from the Chaquopy source dir without Android or Gradle.
pythonpath = [".", "app/src/main/python"]
testpaths = ["tests"]
