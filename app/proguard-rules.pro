# Add project-specific ProGuard rules here as the release build is set up.
# Chaquopy needs its own consumer rules (bundled automatically by the
# plugin) to avoid stripping Python-called classes when minification is
# enabled — see https://chaquo.com/chaquopy/doc/current/android.html#proguard
